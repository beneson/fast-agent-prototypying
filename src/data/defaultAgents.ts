// IDs dos agentes default (indeletáveis)
// Estes agentes são gerenciados no banco de dados Supabase.
// Este arquivo serve apenas como referência dos IDs protegidos.
export const DEFAULT_AGENT_IDS = [
  'agent-mercado-livre',
  'agent-ifood',
  'agent-shopee',
] as const
